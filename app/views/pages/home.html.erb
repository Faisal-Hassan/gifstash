<div class="jumbotron  center">
	<%= image_tag "gif.png" %>
	<br />
	<br />
	<p>Welcome to Gif Stash, the world's catalog of gifs</p>
	<br />
	<% if user_signed_in? %>
		<!-- do something -->
	<% else %>
	<p>
		<%= link_to 'Sign Up', new_user_registration_path, class: 'btn btn-primary btn-lg' %>
	</p>
	<% end %>
</div>